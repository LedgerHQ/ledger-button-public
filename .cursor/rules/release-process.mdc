---
description: Release process — how to cut and publish a new version
alwaysApply: true
---

# Release Process

When the user asks to do a release, cut a version, or publish packages, follow the `/release` command defined in `.cursor/commands/release.md`.

Do not improvise the release steps — always defer to that command.
