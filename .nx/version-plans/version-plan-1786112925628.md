---
"@ledgerhq/ledger-wallet-provider-core": patch
---

Align feature folders with Wallet Experience conventions: rename `usecase`/`usecases` to `use-case`, flatten nested `MigrateDbUseCase`, and move consent into `service/` + `di/`.
