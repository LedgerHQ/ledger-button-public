---
"@ledgerhq/ledger-wallet-provider-core": patch
"@ledgerhq/ledger-wallet-provider": patch
---

Fall back to the default Ledger config in the dApp Config v2 stub when the current dApp isn't onboarded yet, instead of failing initialization entirely.
