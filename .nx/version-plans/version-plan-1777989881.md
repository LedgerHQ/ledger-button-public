---
"@ledgerhq/ledger-wallet-provider": minor
"@ledgerhq/ledger-wallet-provider-core": minor
---

Add refresh and add-account buttons in the select account modal, and a `forceRefresh` option on `getAccounts` to re-fetch accounts from cloud sync
