---
"@ledgerhq/ledger-wallet-provider-core": patch
---

Replace deep relative imports with the `@api/*`, `@internal/*` and `@schemas/*` path aliases. The public API is unchanged: the build rewrites the aliases back to relative paths, and only the ordering of statements inside the bundle and the declarations shifts, as it follows import order.
