---
"@ledgerhq/ledger-wallet-provider-core": patch
---

Flatten `ledger-solana-wallet` into `solana-provider` and move Inversify wiring into `di/` to align with Wallet Experience project structure conventions.
