---
"@ledgerhq/ledger-wallet-provider": patch
"@ledgerhq/ledger-wallet-provider-core": patch
"@ledgerhq/ledger-wallet-provider-evm": patch
"@ledgerhq/ledger-wallet-provider-solana": patch
---

Keep runtime dependencies external instead of inlining them, so a dApp loads a single copy of the core, the Device Management Kit and RxJS. `rxjs`, `xstate`, `purify-ts` and `@ledgerhq/device-management-kit` are now peer dependencies of the provider packages; npm 7+, pnpm and Yarn Berry install them automatically.
