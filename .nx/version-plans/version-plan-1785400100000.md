---
"@ledgerhq/ledger-wallet-provider-core": patch
---

Move account list derivation into the core: new `observeAccountGroups()` stream emits accounts already grouped by address, sorted and filtered by search, with their total fiat value and display tokens computed, and new `observeNetworksForSelectedAddress()` / `selectAccountForNetwork()` expose the network breakdown consumers used to rebuild themselves.
