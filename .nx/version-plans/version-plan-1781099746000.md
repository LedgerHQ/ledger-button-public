---
"@ledgerhq/ledger-wallet-provider": minor
---

Display the package version in a footer at the bottom of the Settings screen. The version is read dynamically from package.json so it always reflects the published version.
